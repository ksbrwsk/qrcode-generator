package de.ksbrwsk.qrcode.model;

/**
 * Enum representing the type of a vCard phone number or address entry.
 * Used as a type qualifier in {@link QrCodeVCard}.
 */
public enum QrCodeEnumType {
    /** Work contact type. */
    WORK,
    /** Home / private contact type. */
    HOME
}
